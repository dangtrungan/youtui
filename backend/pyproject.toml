[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "rhytui-backend"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "ytmusicapi>=1.12.0",
]

[tool.setuptools.packages.find]
where = ["src"]
